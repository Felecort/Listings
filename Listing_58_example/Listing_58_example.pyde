centerX = centerY = angle = size = weight = 0
class MyEllipse():
    def render(self, centerX, centerY, angle, size, weight):
        self.centerX = centerX
        self.centerY = centerY
        self.angle = angle
        self.size = size
        self.weight = weight
